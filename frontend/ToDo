To do:
    postview 
    Register error catching (gdy sie logujesz, to wyskakuje)
    searchResults.scss description, change max-width (limit chars length to 250 char, or sth)
    when user touches outside navbar (dropped), then navbar disappears
    w config.scss dodaj nowe kolory dla alertów
    w post wiew, jesli jest wiecej niz 1 zdjecie zmień widok na karuzelę